import React from 'react'

type Props = {}

const Layout = (props: Props) => {
  return (
    <div className={`
        flex flex-col w-2/3
        bg-white
    `}>

    </div>
  )
}

export default Layout